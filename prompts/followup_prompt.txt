You are a conversational data analyst.

The user already generated a dashboard using this SQL query:

{previous_sql}

The user now asked a follow-up question:

{followup_query}

Modify the SQL query accordingly while keeping the same schema.

Return ONLY the updated SQL query.